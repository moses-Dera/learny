services:
  mailpit:
    image: axllent/mailpit
    container_name: lms-mailpit
    restart: unless-stopped
    ports:
      - 8025:8025
      - 1025:1025
    environment:
      MP_MAX_MESSAGES: 5000
